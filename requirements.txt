# Core dependencies
pandas>=2.0.0
python-dotenv>=1.0.0

# Databricks dependencies (requer credenciais configuradas no .env)
databricks-vectorsearch>=0.22
databricks-langchain>=0.1.0

# AWS Bedrock dependencies
boto3>=1.28.0
botocore>=1.31.0

# Busca fuzzy
rapidfuzz>=3.0.0

# FastAPI and Web Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Opcional: Error tracking (apenas para ambientes Databricks)
# bugsnag>=4.0.0
